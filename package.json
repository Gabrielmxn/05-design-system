{
  "name": "@mxn-ignite-ui/global",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=!@mxn-ignite-ui/docs && changeset publish"
  },
  "dependencies": {
    "phosphor-react": "^1.4.1",
    "turbo": "^2.3.3"
  },
  "packageManager": "npm@10.7.0",
  "devDependencies": {
    "@changesets/cli": "^2.27.11"
  }
}
